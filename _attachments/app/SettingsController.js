define(['angular.route', 'underscore', 'moment'], function (angular, _, moment) {
    return angular.module('fiveOClock').controller('SettingsController', function ($scope, $q, $rootScope, $http, $timeout, $routeParams) {
    });
});